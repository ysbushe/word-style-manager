# Word 样式管理器使用说明

## 一、程序用途

Word 样式管理器 v0.3.0 用于清理、提取和编辑 Word/WPS 文档样式，
支持 `.doc`、`.docx` 和 `.dotx`。

- `.docx`、`.dotx` 可直接处理。
- `.doc` 会自动调用本机 WPS Writer 或 Microsoft Word 转换。
- 所有处理操作均生成新文件，不覆盖原文件。

## 二、添加文档

1. 点击主窗口顶部的“添加文件”或“添加文件夹”。
2. 文档加入后，程序会自动读取样式与编号。
3. 读取期间会显示黄色“文档样式加载中...”。
4. 批量添加后，单击顶部文档列表中的文件即可切换当前预览。
5. “移除选中”只会从任务列表移除文档，不会删除磁盘上的源文件。

程序每次启动时文档列表为空，不会自动恢复上次导入的文件。

顶部文档栏采用紧凑布局，右侧四个按钮分别用于添加、批量添加、移除当前任务和清空任务。列表较长时可使用右侧滚动条。

## 三、样式清理

1. 添加一个或多个待处理文档。
2. 在“样式清理”页选择需要执行的清理项目。
3. 查看样式状态、使用次数及系统样式备注。
4. 可直接点击“全部、未使用、可清理、系统可隐藏、使用中、自定义、带编号”切换列表。
5. 已隐藏的未使用系统样式默认不显示，可打开“显示已隐藏系统样式”查看。
6. 点击“清理选中文档”只处理顶部当前选中的文件。
7. 点击“批量清理全部文档”处理顶部列表中的全部文件。

未使用的普通样式显示为浅红色。部分系统样式由 WPS/Word 自动维护，即使清理也可能重新出现，程序会在“备注”列中提示。

启用“隐藏未使用系统样式”后，程序会保留这些系统样式，但将其标记为默认隐藏；样式以后被真正使用时，WPS/Word 可重新显示它。

## 四、提取模板

1. 在顶部文档列表中选择源文档。
2. 切换到“提取模板”。
3. 输入模板名称。程序固定生成一个标准 Word 模板（`.dotx`）。
4. 默认选择全部样式。双击样式行可取消或重新选择；也可使用“全选”“只选已用”“清空”。
5. 默认启用“自动清理后再保存模板”，关闭后会保留未使用样式与编号。
6. 点击“保存当前文档模板”，或点击“批量保存全部模板”处理顶部列表全部文档。

提取结果统一保存到“设置”中指定的模板库目录。模板只保留样式、编号、主题和字体表等必要内容，不复制源文档的正文、普通图片、页眉页脚及嵌入对象，因此文件通常远小于源文档。

## 五、模板库

模板库用于预览和编辑已保存模板。

- “刷新”：重新读取模板目录。
- “打开目录”：打开模板库存放位置。
- “删除模板”：二次确认后删除模板库中的模板文件和索引记录。
- “编辑模板”：打开独立模板编辑器。

程序每次进入“模板库”时都会核对模板文件。已被手动删除或移动的模板会自动从列表和模板索引中移除；模板索引只记录文件名，因此绿色程序移动目录后仍可继续使用。

模板编辑器默认在主窗口中央打开。系统样式使用中文名称显示；字号采用号数/磅，段落间距、缩进和行距支持厘米、毫米、磅、行、字符和倍数等常用单位。

选择样式后可实时查看排版变化。“保存当前样式修改”立即生成新模板版本；勾选“覆盖原模板”后才会写回当前模板。“撤销样式修改”恢复当前样式打开时的值。

## 六、设置

点击右上角“设置”可调整：

- 输出位置：跟随源文件目录或选择固定目录。
- 模板库存放位置。首次使用默认为 Windows“我的文档\模板库”，也可自行选择其他目录。
- 添加文件夹时是否包含子目录。

当前输出模式会以实色按钮显示。

## 七、更新与帮助

- “检查更新”：手动检查 GitHub Release。
- “GitHub”：打开项目仓库。
- “使用说明”：打开本文档。

程序不会自动联网检查更新，只有点击“检查更新”时才会访问 GitHub。

绿色版更新会先校验下载大小、ZIP 完整性和解压路径，再备份当前程序文件并替换。更新失败时会尝试恢复旧版。程序设置保存在程序目录中；模板默认保存在“我的文档\模板库”，不会因更新或移动绿色版而丢失。

旧版若将模板保存在程序旁的 `templates` 目录，新版首次启动时会复制其中的 `.dotx` 文件到默认模板库，已有同名文件不会被覆盖。

## 八、注意事项

- 处理前无需复制原文件，程序始终生成新文件。
- 请勿在处理过程中关闭程序或移动源文件。
- `.doc` 处理需要安装 WPS Writer 或 Microsoft Word。
- 大批量文档会在后台依次预读，当前选中文档优先显示。
- 正式绿色版应同时包含 `LICENSE` 和 `THIRD_PARTY_NOTICES.md`。
